{
  "lexicon": 1,
  "id": "net.openfederation.account.confirmPasswordReset",
  "description": "Confirm a password reset using the token from the reset email.",
  "defs": {
    "main": {
      "type": "procedure",
      "description": "Verifies the reset token and sets the new password. Revokes all active sessions.",
      "input": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["token", "newPassword"],
          "properties": {
            "token": { "type": "string", "description": "The reset token from the email link." },
            "newPassword": { "type": "string", "description": "The new password to set." }
          }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["success"],
          "properties": {
            "success": { "type": "boolean" }
          }
        }
      },
      "errors": [
        { "name": "InvalidToken", "description": "Token is invalid, expired, or already used." },
        { "name": "WeakPassword", "description": "New password does not meet strength requirements." }
      ]
    }
  }
}
