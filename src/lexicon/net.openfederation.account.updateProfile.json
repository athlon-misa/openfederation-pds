{
  "lexicon": 1,
  "id": "net.openfederation.account.updateProfile",
  "revision": 2,
  "description": "Update the authenticated user's profile. Supports both standard ATProto profile fields and custom collection data.",
  "defs": {
    "main": {
      "type": "procedure",
      "description": "Update profile fields in app.bsky.actor.profile or write to a custom profile collection.",
      "input": {
        "encoding": "application/json",
        "schema": {
          "type": "ref",
          "ref": "#input"
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "ref",
          "ref": "#output"
        }
      },
      "errors": [
        {
          "name": "InvalidRequest",
          "description": "Validation failed."
        },
        {
          "name": "InvalidBlobRef",
          "description": "avatar/banner is not the blob object uploadBlob returned, or references a blob this account did not upload."
        }
      ]
    },
    "input": {
      "type": "object",
      "properties": {
        "displayName": {
          "type": "string",
          "description": "Display name."
        },
        "description": {
          "type": "string",
          "description": "Bio / description."
        },
        "collection": {
          "type": "string",
          "description": "Optional custom collection NSID (e.g., app.grvty.actor.profile). Defaults to app.bsky.actor.profile."
        },
        "record": {
          "type": "unknown",
          "description": "Full record to write when using a custom collection. Ignored for app.bsky.actor.profile."
        },
        "avatar": {
          "type": "unknown",
          "description": "ATProto blob ref for the profile avatar — the exact object com.atproto.repo.uploadBlob returned, which must belong to the caller's DID. null removes it."
        },
        "banner": {
          "type": "unknown",
          "description": "ATProto blob ref for the profile banner; same rules as avatar."
        }
      }
    },
    "output": {
      "type": "object",
      "properties": {
        "uri": {
          "type": "string"
        },
        "cid": {
          "type": "string"
        }
      },
      "required": [
        "uri",
        "cid"
      ]
    }
  }
}
